#include <bits/stdc++.h>
using namespace std;

long long lcm(long long a, long long b) {
    return (a / __gcd(a, b)) * b;
}

int main() {
    long long X, Y, Z, K;
    cin >> X >> Y >> Z >> K;

    long long l = lcm(lcm(X, Y), Z);

    if (l < K)
        cout << "Crash";
    else
        cout << "Safe";

    return 0;
}
